export type CardTypes = {
    name: string,
    parag: string,
    auth_name: string,
    img: string,
    auth_img: string,
    id: string,
}

export type CommTypes = {
    username: string;
    parag: string;
}

